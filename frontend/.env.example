# Base URL of the FastAPI backend. Leave unset in development — Vite proxies
# /api to http://localhost:8000 (see vite.config.ts). Set this when the
# frontend and backend are deployed separately (e.g. static hosting + a
# backend on Render/Railway/Fly.io).
VITE_API_BASE_URL=
